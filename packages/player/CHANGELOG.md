# @ailuracode/alpine-player

## 0.1.0

### Minor Changes

- Initial release: Alpine.js `x-audio` and `x-video` directives with shared `PlayerController`, `AudioController`, and `VideoController` for native media playback. Includes reactive scope bindings, fullscreen and Picture-in-Picture support for video, SSR-safe imports, and standalone controller factories.
