# @ailuracode/alpine-export

## 0.1.0

### Minor Changes

- 8192ffd: Add `@ailuracode/alpine-export` — Alpine.js magic `$export` for programmatic file exports (URLs, blobs, files, and plain text). Published as `alpine-export` because npm blocks new packages containing the word "download".
