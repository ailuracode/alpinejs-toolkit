/**
 * Public entrypoint for `@ailuracode/alpine-dialog`.
 *
 * Re-exports only. Implementation lives in `./plugin.ts`,
 * `./store.ts`, and `./focus.ts`.
 */

export * from "./plugin.js";
export { default } from "./plugin.js";
