/**
 * Public entrypoint for `@ailuracode/alpine-json-api`.
 *
 * Re-exports only. Implementation lives in `./plugin.ts` and
 * supporting JSON:API modules under `./*.ts`.
 */

export * from "./plugin.js";
export { default } from "./plugin.js";
