---
"@ailuracode/alpine-keyboard": minor
---

Add `@ailuracode/alpine-keyboard` — headless scoped keyboard shortcut registry with chords, sequences, conflict resolution, Alpine `$keyboard` integration, and standalone `KeyboardController`.
