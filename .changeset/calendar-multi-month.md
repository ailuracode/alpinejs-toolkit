---
"@ailuracode/alpine-calendar": minor
---

Add `numberOfMonths` option and `months[]` multi-month grid API. Navigation moves the anchor by `numberOfMonths` months. `weeks` remains the first month grid for backward compatibility.
