---
"@ailuracode/alpine-calendar": patch
---

Add flexible `disabled` matchers (single days, inclusive/exclusive ranges, `only` ranges, weekdays, custom predicates) and `dateFns` context passthrough for date-fns plugins such as `@date-fns/tz`.
