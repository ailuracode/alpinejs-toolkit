---
"@ailuracode/alpine-timer": minor
---

Add `@ailuracode/alpine-timer` with `$timer.create()`, `$timer.countdown()`, `$timer.countup()`, and `$timer.stopwatch()` presets backed by a drift-resistant timing engine.
